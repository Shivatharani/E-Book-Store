<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>SHIVA'S E BOOK STORE - Home</title>
  <link rel="stylesheet" href="/css/style.css" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />


  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background-color: #eef6ff;
      margin: 0;
      padding: 0;
      display: flex;
      min-height: 100vh;
    }


    .page-content {
      margin-left: 220px;
      flex-grow: 1;
      padding: 20px;
      transition: margin-left 0.3s ease;
    }


    h1 {
      text-align: center;
      color: #003366;
      margin: 20px 0;
      font-size: 2rem;
    }


    /* Filter bar */
    #filterForm {
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 10px;
      margin: 20px 0 30px;
      flex-wrap: wrap;
    }


    #filterForm input[type="text"], #filterForm select {
      padding: 8px 10px;
      font-size: 1rem;
      border: 1px solid #bbb;
      border-radius: 6px;
      outline: none;
      transition: border 0.3s ease;
    }
    #filterForm input[type="text"]:focus {
      border-color: #0078ff;
    }


    #filterForm button {
      padding: 8px 14px;
      background-color: #00509e;
      color: white;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      font-weight: 600;
      transition: background 0.3s ease, transform 0.2s ease;
    }
    #filterForm button:hover {
      background-color: #003d7a;
      transform: translateY(-2px);
    }


    /* Grid layout */
    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(230px, 1fr));
      gap: 25px;
      padding: 20px;
      justify-items: center;
      align-items: start;
      margin: 0 auto;
      width: 100%;
      max-width: 1200px;
    }


    /* Book card */
    .book-card {
      background: #fff;
      border-radius: 10px;
      box-shadow: 0 4px 15px rgba(0,0,0,0.1);
      width: 230px;
      padding: 15px;
      text-align: center;
      transition: all 0.3s ease;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }
    .book-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 8px 18px rgba(0,0,0,0.15);
    }


    .book-card img {
      width: 100%;
      height: 260px;
      object-fit: cover;
      border-radius: 6px;
      margin-bottom: 12px;
    }


    .book-card h3 {
      font-size: 1.05rem;
      margin: 5px 0;
    }


    .book-card a {
      text-decoration: none;
      color: #0033cc;
      font-weight: 600;
      transition: color 0.3s ease;
    }
    .book-card a:hover {
      color: #001f80;
    }


    .book-card p {
      font-size: 0.95rem;
      color: #333;
      margin: 6px 0;
    }


    /* Add to cart button */
    .cart-btn {
      margin-top: 10px;
      background: linear-gradient(135deg, #00509e, #007bff);
      color: #fff;
      border: none;
      padding: 8px 14px;
      border-radius: 6px;
      cursor: pointer;
      font-weight: 600;
      font-size: 0.95rem;
      transition: all 0.3s ease;
    }
    .cart-btn:hover {
      background: linear-gradient(135deg, #007bff, #60a9ff);
      transform: scale(1.05);
    }


    /* Empty state */
    .empty-message {
      text-align: center;
      font-size: 1.2rem;
      color: #888;
      margin-top: 50px;
    }


    /* Responsive */
    @media (max-width: 768px) {
      .page-content {
        margin-left: 60px;
      }
      .book-card {
        width: 90%;
      }
    }
  </style>
</head>


<body>
  <%- include('partials/sidebar') %>


  <div class="page-content">
    <h1>📚 Welcome to SHIVA'S E BOOK STORE</h1>


    <form id="filterForm" method="GET" action="/home">
      <input type="text" name="search" placeholder="Search books..." value="<%= search %>" />
      <select name="category" onchange="this.form.submit()">
        <option <%= category === 'All' ? 'selected' : '' %>>All</option>
      </select>
      <button type="submit"><i class="fas fa-search"></i> Search</button>
    </form>


    <div class="grid">
      <% if (books && books.length > 0) { %>
        <% books.forEach(book => { %>
          <div class="book-card">
            <img src="<%= book.coverImage %>" alt="<%= book.title %>" />
            <h3><a href="/books/<%= book._id %>"><%= book.title %></a></h3>
            <p>Price: ₹<%= book.price %></p>
            <p>Author: <a href="/authors/<%= book.author._id %>"><%= book.author.name %></a></p>
            <form action="/add-to-cart/<%= book._id %>" method="POST">
              <button type="submit" class="cart-btn">
                <i class="fas fa-cart-plus"></i> Add to Cart
              </button>
            </form>
          </div>
        <% }) %>
      <% } else { %>
        <p class="empty-message">No books found. Try searching with different keywords.</p>
      <% } %>
    </div>
  </div>
</body>
</html>