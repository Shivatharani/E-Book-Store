<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>SHIVA'S E BOOK STORE - Home</title>
  <link rel="stylesheet" href="/css/style.css" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #eef6ff;
      margin: 0;
      padding: 0;
      display: flex;
      min-height: 100vh;
    }
    .page-content {
      margin-left: 220px;
      flex-grow: 1;
      padding: 20px;
    }
    form#filterForm {
      margin-bottom: 20px;
    }
    input[type="text"], select {
      padding: 8px;
      font-size: 1rem;
    }
    button[type="submit"] {
      padding: 8px 12px;
      font-size: 1rem;
      background-color: #00509e;
      color: white;
      border: none;
      cursor: pointer;
    }
    button[type="submit"]:hover {
      background-color: #003d7a;
    }
    .grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 25px;
  justify-items: center;
  padding: 20px;
  width: 100%;
  max-width: 900px;
  margin: 0 auto;
}
    .book-card {
      background: white;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
      width: 200px;
      padding: 15px;
      text-align: center;
      transition: transform 0.2s ease-in-out;
    }
    .book-card:hover {
      transform: scale(1.05);
    }
    .book-card img {
      width: 100%;
      height: 250px;
      object-fit: cover;
      border-radius: 5px;
    }
    a {
      text-decoration: none;
      font-weight: bold;
      color: #0033cc;
    }
  </style>
</head>
<body>
  <%- include('partials/sidebar') %>

  <div class="page-content">
    <h1>📚 Welcome to SHIVA'S E BOOK STORE</h1>

    <form id="filterForm" method="GET" action="/home">
      <input type="text" name="search" placeholder="Search books..." value="<%= search %>" />
      <select name="category" onchange="this.form.submit()">
        <option <%= category === 'All' ? 'selected' : '' %>>All</option>
        <option <%= category === 'New Arrivals' ? 'selected' : '' %>>New Arrivals</option>
        <option <%= category === 'Bestsellers' ? 'selected' : '' %>>Bestsellers</option>
        <option <%= category === 'Genres' ? 'selected' : '' %>>Genres</option>
      </select>
      <button type="submit">Search</button>
    </form>

    <div class="grid">
      <% books.forEach(book => { %>
      <div class="book-card">
        <form action="/add-to-cart/<%= book._id %>" method="POST">
          <button type="submit">🛒 Add to Cart</button>
        </form>
        <img src="<%= book.coverImage %>" alt="<%= book.title %>" />
        <h3><a href="/books/<%= book._id %>"><%= book.title %></a></h3>
        <p>Price: ₹<%= book.price %></p>
        <p>Author: <a href="/authors/<%= book._id %>"><%= book.author.name %></a></p>
      </div>
      <% }) %>
    </div>
  </div>
</body>
</html>
