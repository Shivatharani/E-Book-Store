<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= author.name %> - SHIVA'S E BOOK STORE</title>
  <link rel="stylesheet" href="/css/style.css">
  <style>
    .author-container {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: flex-start;
      padding: 40px;
      background: #f8f9fa;
      border-radius: 10px;
      max-width: 480px;
      margin: 50px auto;
      box-shadow: 0 5px 15px rgba(0,0,0,0.11);
    }
    .author-container img {
      width: 150px;
      height: 150px;
      border-radius: 50%;
      object-fit: cover;
      margin-bottom: 20px;
      box-shadow: 0 2px 15px rgba(0,0,0,0.15);
      border: 3px solid #fff;
      background: #eef6ff;
    }
    .author-container h2 {
      margin-bottom: 10px;
      font-size: 2rem;
      color: #003366;
    }
    .author-container p {
      color: #333;
      font-size: 1.07rem;
      margin-bottom: 16px;
      text-align: center;
    }
    .author-container a {
      display: inline-block;
      margin-top: 14px;
      text-decoration: none;
      color: #00509e;
      font-weight: bold;
    }
    .author-container a:hover {
      text-decoration: underline;
      color: #003d7a;
    }
  </style>
</head>
<body>
  <%- include('partials/sidebar') %>
  <div class="page-content">
    <div class="author-container">
      <img src="<%= author.image %>" alt="<%= author.name %>">
      <h2><%= author.name %></h2>
      <p><%= author.bio || 'Biography not available' %></p>
      <a href="/home">🔙 Back to Home</a>
    </div>
  </div>
</body>
</html>